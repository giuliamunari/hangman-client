/**
 * Reducer for all the actions used for the game
 */

const initialState = {}

export default function (state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}